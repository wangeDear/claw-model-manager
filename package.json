{
  "name": "claw-model-manager",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && mkdir -p dist/public && cp -r src/public/* dist/public/",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit",
    "start:backend": "node dist/backend.js",
    "dev:backend": "tsx src/backend.ts"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4",
    "tsx": "^4.19.1"
  }
}
